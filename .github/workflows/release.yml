# Create a GitHub upstream release when a version tag is pushed, and attach
# the source tarball, a zip of the built page for installing by hand, and a
# binary package for each distribution family.
#
# The RPM has to be built on Fedora and the deb on Debian, so they cannot
# share a job: the source job creates the release and attaches the tarball and
# the RPM, and the deb job uploads to that release once it exists.
name: release
on:
  push:
    tags:
      # this is a glob, not a regexp
      - '[0-9]*'
  # Re-run against a tag that already exists, after fixing whatever made an
  # earlier attempt fail. Assets are uploaded into the existing release.
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to build and attach assets to'
        required: true
jobs:
  source:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/cockpit-project/tasks:latest
      options: --user root
    permissions:
      # create GitHub release
      contents: write
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Clone repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ref: ${{ inputs.tag || github.ref }}

      - name: Install gh
        run: dnf install -y gh

      # https://github.blog/2022-04-12-git-security-vulnerability-announced/
      # This has to name the checkout itself; a parent directory does not match.
      - name: Pacify git's permission check
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Workaround for https://github.com/actions/checkout/pull/697
        run: |
          TAG_VERSION="$(git describe --tags)"
          git fetch --force origin ${TAG_VERSION}:refs/tags/${TAG_VERSION}

      - name: Build release
        run: make dist

      - name: Build zip
        run: make zip

      # The spec exists once `make dist` has run, so its BuildRequires can be
      # resolved from it rather than duplicated here.
      - name: Install RPM build dependencies
        run: |
          dnf install -y rpm-build 'dnf-command(builddep)'
          dnf builddep -y cockpit-samba.spec

      - name: Build RPM
        run: make rpm

      - name: Create a release
        run: |
          set -eux

          TAG_VERSION="$(git describe --tags)"
          REPO_NAME="${GITHUB_REPOSITORY#*/}"

          # The source tarball keeps its versioned name: the RPM spec fetches it
          # by version in Source0, and packit builds from it.
          ASSETS="${REPO_NAME}-${TAG_VERSION}.tar.xz"

          # The packages get a fixed name, so that
          #   /releases/latest/download/cockpit-samba-latest.rpm
          # can be fetched by a script that does not know the version. Each
          # package still records its real version internally.
          cp "$(ls *.noarch.rpm)" "${REPO_NAME}-latest.rpm"
          cp "${REPO_NAME}-${TAG_VERSION}.zip" "${REPO_NAME}-latest.zip"
          ASSETS="${ASSETS} ${REPO_NAME}-latest.rpm ${REPO_NAME}-latest.zip"

          # The release can already exist: publishing one from the GitHub web
          # interface creates the tag, which is what triggers this workflow,
          # and re-running the job after a failure lands here too. Upload into
          # it rather than failing.
          if gh release view "${TAG_VERSION}" >/dev/null 2>&1; then
              gh release upload "${TAG_VERSION}" ${ASSETS} --clobber
          else
              # The release notes are the body of the annotated tag.
              git tag -l --format='%(contents:body)' "${TAG_VERSION}" > release-note.txt
              gh release create --title "${TAG_VERSION}" --notes-file release-note.txt \
                "${TAG_VERSION}" ${ASSETS}
          fi

  deb:
    needs: source
    runs-on: ubuntu-latest
    permissions:
      # upload to the release the source job created
      contents: write
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Clone repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ref: ${{ inputs.tag || github.ref }}

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends debhelper fakeroot gettext

      - name: Workaround for https://github.com/actions/checkout/pull/697
        run: |
          TAG_VERSION="$(git describe --tags)"
          git fetch --force origin ${TAG_VERSION}:refs/tags/${TAG_VERSION}

      - name: Build Debian package
        run: make deb

      - name: Attach it to the release
        run: |
          set -eux
          REPO_NAME="${GITHUB_REPOSITORY#*/}"
          # Fixed name, matching the other packages; see the source job.
          cp "$(ls *.deb)" "${REPO_NAME}-latest.deb"
          gh release upload "$(git describe --tags)" "${REPO_NAME}-latest.deb" --clobber
